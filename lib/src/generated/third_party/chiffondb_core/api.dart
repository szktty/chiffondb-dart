// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.12.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../../frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';

// These types are ignored because they are neither used by any `pub` functions nor (for structs and enums) marked `#[frb(unignore)]`: `NodeResult`, `Transaction`
// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `drop`
// These functions are ignored (category: IgnoreBecauseOwnerTyShouldIgnore): `apply_schema`, `commit`, `count_edges`, `count_nodes`, `delete_edge`, `delete_node`, `edge_exists`, `get_edge_properties`, `get_node_properties`, `insert_edge`, `insert_node`, `list_edges`, `list_nodes`, `node_exists`, `patch_edge_properties`, `patch_node_properties`, `rollback`, `update_edge_properties`, `update_node_properties`

// Rust type: RustOpaqueMoi<flutter_rust_bridge::for_generated::RustAutoOpaqueInner<Connection>>
abstract class Connection implements RustOpaqueInterface {
  /// Adds an additional label to an edge (by type name).
  Future<void> addEdgeLabel({required RecordId rid, required String typeName});

  /// Adds a label to an edge, registering the name if unknown.
  /// Returns `{id, created}` as a JSON object string.
  Future<String> addEdgeLabelDynamic({
    required RecordId rid,
    required String typeName,
  });

  /// Adds an additional label to a node (by type name).
  Future<void> addNodeLabel({required RecordId rid, required String typeName});

  /// Adds a label to a node, registering the name if unknown.
  /// Returns `{id, created}` as a JSON object string.
  Future<String> addNodeLabelDynamic({
    required RecordId rid,
    required String typeName,
  });

  /// Parses, validates, and saves the schema text to the database.
  Future<void> applySchema({required String schemaText});

  /// Begins a transaction. Returns an error if one is already in progress.
  Future<void> beginTransaction();

  /// Flushes pending writes and releases the database (file lock included).
  /// After this call the connection is closed; any further method call returns an error.
  /// If a transaction is pending, it is rolled back before the flush.
  Future<void> close();

  /// Commits the current transaction, flushing all writes to disk.
  Future<void> commitTransaction();

  /// Returns the connecting subgraph between multiple nodes.
  /// Result is JSON in the form `{"nodes":[...],"edges":[...]}`.
  Future<String> connectingSubgraph({
    required List<RecordId> nodeRids,
    required BigInt maxDepth,
    required String direction,
    List<String>? edgeLabels,
  });

  /// Returns the number of edges, optionally filtered by type_name.
  Future<BigInt> countEdges({String? typeName});

  /// Returns the number of nodes, optionally filtered by type_name.
  Future<BigInt> countNodes({String? typeName});

  /// Creates a new database and opens it.
  static Future<Connection> create({required String path}) =>
      RustLib.instance.api.chiffondbCoreApiConnectionCreate(path: path);

  /// Creates a new database with a custom in-memory page-cache budget (bytes).
  static Future<Connection> createWithMaxMemory({
    required String path,
    required BigInt maxMemoryBytes,
  }) => RustLib.instance.api.chiffondbCoreApiConnectionCreateWithMaxMemory(
    path: path,
    maxMemoryBytes: maxMemoryBytes,
  );

  /// Deletes an edge.
  Future<void> deleteEdge({required RecordId rid});

  /// Deletes a node (with cascade deletion of associated edges).
  Future<void> deleteNode({required RecordId rid});

  /// Returns true if an edge with the given RecordId exists.
  Future<bool> edgeExists({required RecordId rid});

  /// Returns the names of all edge types defined in the schema.
  Future<List<String>> edgeTypeNames();

  /// Returns the property definitions for an edge type as a JSON string.
  /// Returns `"null"` if the type is not found.
  Future<String> edgeTypeSchema({required String typeName});

  /// Executes a Cypher query and returns the result as JSON.
  Future<String> executeCypher({required String query});

  /// Executes a traversal command and returns the result as JSON.
  /// Returns a JSON array for CollectResult::Rows, or `{"count": N}` for Count.
  Future<String> executeTraversal({required String commandJson});

  /// Returns the source and destination RecordIds of an edge as a JSON string: `{"from":{...},"to":{...}}`.
  Future<String> getEdgeEndpoints({required RecordId rid});

  /// Returns all label names of an edge as a JSON array string.
  Future<String> getEdgeLabels({required RecordId rid});

  /// Returns edge properties as a JSON string.
  Future<String> getEdgeProperties({required RecordId rid});

  /// Returns the primary type name of an edge.
  Future<String> getEdgeTypeName({required RecordId rid});

  /// Returns all label names of a node as a JSON array string.
  Future<String> getNodeLabels({required RecordId rid});

  /// Returns node properties as a JSON string.
  Future<String> getNodeProperties({required RecordId rid});

  /// Returns the primary type name of a node.
  Future<String> getNodeTypeName({required RecordId rid});

  /// Returns the schema stored in the database as DSL source text.
  Future<String> getSchemaText();

  /// Inserts an edge by type name.
  Future<RecordId> insertEdge({
    required String typeName,
    required RecordId from,
    required RecordId to,
    required String propsJson,
  });

  /// Inserts a node by type name.
  Future<RecordId> insertNode({
    required String typeName,
    required String propsJson,
  });

  /// Inserts a node, registering any unknown label names on the fly. Returns the new node's
  /// RecordId and a JSON object string mapping each label name to `{id, created}`.
  Future<DynamicInsertResult> insertNodeWithDynamicLabels({
    required String primaryType,
    required List<String> additionalLabels,
    required String propsJson,
  });

  /// Inserts a node with multiple labels.
  Future<RecordId> insertNodeWithLabels({
    required String primaryType,
    required List<String> additionalLabels,
    required String propsJson,
  });

  /// Returns all edge RecordIds and their properties as a JSON string. Optionally filtered by type_name.
  Future<String> listEdges({String? typeName});

  /// Returns all node RecordIds and their properties as a JSON string. Optionally filtered by type_name.
  Future<String> listNodes({String? typeName});

  /// Returns true if a node with the given RecordId exists.
  Future<bool> nodeExists({required RecordId rid});

  /// Returns the names of all node types defined in the schema.
  Future<List<String>> nodeTypeNames();

  /// Returns the property definitions for a node type as a JSON string.
  /// Returns `"null"` if the type is not found.
  Future<String> nodeTypeSchema({required String typeName});

  /// Opens an existing database.
  static Future<Connection> open({required String path}) =>
      RustLib.instance.api.chiffondbCoreApiConnectionOpen(path: path);

  /// Opens an in-memory database (no file created).
  static Future<Connection> openInMemory() =>
      RustLib.instance.api.chiffondbCoreApiConnectionOpenInMemory();

  /// Opens an existing database with a custom in-memory page-cache budget (bytes).
  static Future<Connection> openWithMaxMemory({
    required String path,
    required BigInt maxMemoryBytes,
  }) => RustLib.instance.api.chiffondbCoreApiConnectionOpenWithMaxMemory(
    path: path,
    maxMemoryBytes: maxMemoryBytes,
  );

  /// Updates only the specified keys in an edge's properties. Unspecified keys are preserved.
  Future<void> patchEdgeProperties({
    required RecordId rid,
    required String patchJson,
  });

  /// Updates only the specified keys in a node's properties. Unspecified keys are preserved.
  Future<void> patchNodeProperties({
    required RecordId rid,
    required String patchJson,
  });

  /// Removes a label from an edge (by type name).
  Future<void> removeEdgeLabel({
    required RecordId rid,
    required String typeName,
  });

  /// Removes a label from a node (by type name).
  Future<void> removeNodeLabel({
    required RecordId rid,
    required String typeName,
  });

  /// Rolls back the current transaction, restoring the pre-begin snapshot.
  Future<void> rollbackTransaction();

  /// Finds the shortest path using BFS.
  /// Returns JSON in the form `{"nodes":[{"page":N,"slot":N},...], "edges":[...]}`.
  /// Returns `None` if no path exists.
  Future<String?> shortestPath({
    required RecordId from,
    required RecordId to,
    required BigInt maxDepth,
    required String direction,
    List<String>? edgeLabels,
  });

  /// Updates edge properties (overwrites existing values).
  Future<void> updateEdgeProperties({
    required RecordId rid,
    required String propsJson,
  });

  /// Updates node properties (overwrites existing values).
  Future<void> updateNodeProperties({
    required RecordId rid,
    required String propsJson,
  });

  /// Vacuums the in-memory database (compacts deleted slots).
  /// For file-backed databases, use `vacuum_file` instead.
  Future<void> vacuum();

  /// Vacuums a file-backed database at the given path.
  /// The database must not be open by any other process.
  static Future<void> vacuumFile({required String path}) =>
      RustLib.instance.api.chiffondbCoreApiConnectionVacuumFile(path: path);

  /// Runs integrity checks and returns a list of warning strings.
  /// An empty list means the database is healthy.
  Future<List<String>> verify();
}

/// Result of a dynamic-label node insert: the new node's RecordId plus a JSON object
/// string mapping each label name to `{"id":<u16>,"created":<bool>}`.
class DynamicInsertResult {
  final RecordId rid;
  final String assignmentsJson;

  const DynamicInsertResult({required this.rid, required this.assignmentsJson});

  @override
  int get hashCode => rid.hashCode ^ assignmentsJson.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DynamicInsertResult &&
          runtimeType == other.runtimeType &&
          rid == other.rid &&
          assignmentsJson == other.assignmentsJson;
}

class RecordId {
  final int page;
  final int slot;

  const RecordId({required this.page, required this.slot});

  @override
  int get hashCode => page.hashCode ^ slot.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is RecordId &&
          runtimeType == other.runtimeType &&
          page == other.page &&
          slot == other.slot;
}
